services:
  - type: web
    name: financas-bot
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: BOT_TOKEN
      - key: MONGODB_URI
